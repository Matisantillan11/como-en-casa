import { Cancel01Icon } from '@hugeicons/core-free-icons';
import { HugeiconsIcon } from '@hugeicons/react';

export default function CloseIcon() {
  return <HugeiconsIcon icon={Cancel01Icon} size={24} color="#000" strokeWidth={2} />;
}
