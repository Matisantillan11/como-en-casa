import { Add01Icon } from '@hugeicons/core-free-icons';
import { HugeiconsIcon } from '@hugeicons/react';

export default function AddIcon() {
  return <HugeiconsIcon icon={Add01Icon} size={24} color="currentColor" strokeWidth={1.5} />;
}
